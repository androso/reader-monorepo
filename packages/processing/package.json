{
    "name": "@reader/processing",
    "version": "0.1.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "prebuild": "pnpm --filter @reader/epub --filter @reader/jobs --filter @reader/providers build",
        "build": "tsc -p tsconfig.json",
        "pretest": "pnpm --filter @reader/epub --filter @reader/jobs --filter @reader/providers build",
        "test": "tsc -p tsconfig.test.json && NODE_PATH=../../apps/api/node_modules:../../packages/epub/node_modules:./node_modules node --test ../../.tmp/packages-processing-test/test/processing.test.js"
    },
    "dependencies": {
        "@reader/epub": "workspace:*",
        "@reader/jobs": "workspace:*",
        "@reader/providers": "workspace:*",
        "jsdom": "^25.0.1",
        "jszip": "^3.10.1",
        "pdf2json": "^3.1.5"
    },
    "devDependencies": {
        "@types/jsdom": "^21.1.7",
        "@types/node": "^22.10.2",
        "typescript": "^5.7.2"
    }
}
