{
    "name": "@reader/jobs",
    "version": "0.1.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "tsc -p tsconfig.test.json && NODE_PATH=./node_modules node --test ../../.tmp/packages-jobs-test/test/jobs.test.js"
    },
    "dependencies": {
        "bullmq": "^5.64.1",
        "ioredis": "^5.4.2"
    },
    "devDependencies": {
        "@types/node": "^22.10.2",
        "typescript": "^5.7.2"
    }
}
